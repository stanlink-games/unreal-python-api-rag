"""Unreal Engine Python API — retrieval / MCP knowledge server.

Grounds any AI agent's UE Editor Python in the real, version-exact `unreal`
API (introspected straight from the engine), served over MCP so it plugs into
Forge, Claude, kiro, GPT — any MCP client.
"""

__version__ = "0.1.0"
